"""StreamingDownloader — terminal downloader for StreamingCommunity and AnimeUnity.

Single process, no server, no background workers, no settings: a URL goes in and
the files come out, one download at a time, finishing when they are on disk.
"""

__version__ = "0.2.0"
